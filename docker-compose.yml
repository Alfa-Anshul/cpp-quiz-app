version: '3.8'
services:
  web:
    image: python:3.9-slim
    container_name: cpp-quiz-app
    working_dir: /app
    command: python -m http.server 8000
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    environment:
      - PYTHONUNBUFFERED=1
